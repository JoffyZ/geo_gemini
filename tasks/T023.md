# T023: Next.js Server Actions for Dashboard Data

## 基本信息

- 所属需求: Phase 5.1: Multi-Dim Aggregation
- 深度/宽度: L2 / M
- 负责人: Claude (gsd-planner)
- 前置: T022 📝 (Materialized Views)

---

## 输入

- `brand_metrics_daily` (Materialized View).
- Dashboard 过滤条件 (Date Range, Countries, Categories, Platforms).

---

## 输出

- `src/lib/actions/analytics.ts`: Server Actions for fetching stats.

---

## 验收标准（技术视角）

- [ ] **聚合查询 API**:
  - [ ] 实现 `getTrendData`：返回按日期排序的品牌提及率和排名数据，适配 Recharts 绘图格式。
  - [ ] 实现 `getKPISummary`：返回 Dashboard 顶部的统计卡片数据 (Total Mentions, Share of Voice, WoW Change)。
- [ ] **多维度过滤逻辑**:
  - [ ] 确保 SQL 查询能够根据传入的数组 (platforms[], countries[]) 动态构造 WHERE 子句。
  - [ ] 严格执行租户隔离 (`tenantId` check)。
- [ ] **异步数据加载策略**:
  - [ ] 利用 React Server Components 的异步特性进行并发获取。
  - [ ] 支持缓存 (Next.js `unstable_cache` or `cache` tag) 以提高高频访问性能。

---

## 状态追踪

- 状态: 📝 planned
- started_at: null
- updated_at: null
- completed_at: null
