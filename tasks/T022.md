# T022: Multi-Dimensional Metrics Aggregation (Materialized Views)

## 基本信息

- 所属需求: Phase 5.1: Multi-Dim Aggregation
- 深度/宽度: L3 / H (High Complexity)
- 负责人: Claude (gsd-planner)
- 前置: T021 📝 (Core Management UI completed)

---

## 输入

- `monitoring_results` 表 (DB): 原始监测结果 JSON。
- `brands`, `prompts`, `categories` 关联表。

---

## 输出

- `src/lib/db/views.ts`: 物化视图定义 SQL。
- `src/db/schema.ts`: Drizzle Mapping for aggregated metrics.

---

## 验收标准（技术视角）

- [ ] **物化视图实现**:
  - [ ] 定义 `brand_metrics_daily` 物化视图。
  - [ ] 逻辑：从 `monitoring_results.content` 提取品牌排名数据，并按 `tenant_id`, `brand_id`, `category_id`, `country_code`, `ai_platform`, `date_day` 聚合。
  - [ ] 核心指标：`mention_count`, `avg_rank`, `share_of_voice` (占位百分比)。
- [ ] **性能优化**:
  - [ ] 创建联合唯一索引，支持 `REFRESH MATERIALIZED VIEW CONCURRENTLY`。
  - [ ] 确保索引覆盖常用过滤维度。
- [ ] **Drizzle 集成**:
  - [ ] 在 `src/db/schema.ts` 中定义对应结构，支持类型安全的查询。
  - [ ] 实现 `refreshBrandMetricsView()` Server Action 供调度使用。

---

## 状态追踪

- 状态: 📝 planned
- started_at: null
- updated_at: null
- completed_at: null
