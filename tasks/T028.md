# T028: Commercial Priority Scoring (CPS) Engine

## 基本信息

- 所属需求: Phase 6.1: CPS Scoring
- 深度/宽度: L2 / S
- 负责人: Claude (gsd-planner)
- 前置: T027 (Volume)

---

## 输入

- `search_volume` (来自 T027)。
- `intent_category` (关键词意图)。

---

## 输出

- `src/lib/volume/cps-engine.ts`: 评分逻辑引擎。
- `src/db/schema.ts`: 增加 `cps_score` 字段。

---

## 状态追踪

- 状态: ⏳ pending
- started_at: —
- updated_at: 2026-03-13
- completed_at: —

---

## 验收标准（技术视角）

- [ ] **评分逻辑**:
  - [ ] 实现 `calculateCPS(volume, intent, trend)` 函数。
  - [ ] 归一化公式：`CPS = (log10(volume) * 0.4) + (intent_weight * 0.4) + (trend * 0.2)`。
- [ ] **数据持久化**:
  - [ ] 在 `prompts` 表中增加 `cps_score` 和 `intent_category` 字段。
- [ ] **执行脚本**:
  - [ ] 提供一个同步脚本，为所有存量问题更新 CPS 分数。
