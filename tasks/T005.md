# T005: Multi-tenant Database Schema Design

## 基本信息

- 所属需求: Phase 1: Infrastructure & Data Model
- 深度/宽度: L3 / M
- 负责人: Rex
- 前置: T004 📋

---

## 输入

- 需求文档：PRD.md
- 技术要求：Drizzle ORM + Supabase PostgreSQL
- 核心实体：Tenants, Categories, Brands, Competitors, Prompts, Results

---

## 输出

- `src/db/schema.ts` 完整 Drizzle Schema 定义
- `drizzle.config.ts` 配置文件
- 第一阶段的迁移脚本 (Generated SQL)

---

## 验收标准（技术视角）

- [x] 定义 `tenants` 表：`id`, `name`, `created_at`
- [x] 定义 `categories` 表：`id`, `tenant_id`, `name`, `description`
- [x] 定义 `brands` 表：`id`, `tenant_id`, `name`, `is_competitor`
- [x] 定义 `prompts` 表：`id`, `tenant_id`, `category_id`, `content`, `is_active`
- [x] 定义 `monitoring_results` 表：
  - 核心字段：`id`, `tenant_id`, `prompt_id`, `ai_platform`, `country_code`, `content`, `raw_response`, `created_at`
- [x] 所有外键必须包含 `tenant_id` 以支持严格的租户隔离
- [x] 在 `src/db/` 目录下创建 `index.ts` 用于初始化 Drizzle 客户端
- [x] 提供 SQL 定义作为参考

---

## 状态追踪

- 状态: ✅ completed
- started_at: 2026-03-09
- updated_at: 2026-03-09
- completed_at: 2026-03-09

---

## 结论

已完成多租户数据库 Schema 设计。

### SQL 参考 (Supabase PostgreSQL)

```sql
-- ENUM 定义
CREATE TYPE ai_platform AS ENUM ('chatgpt', 'perplexity', 'gemini', 'google_ai_overviews');

-- 租户表
CREATE TABLE tenants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
);

-- 分类表
CREATE TABLE categories (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  description TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
);

-- 品牌表
CREATE TABLE brands (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  is_competitor BOOLEAN DEFAULT FALSE NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
);

-- 监控提示词表
CREATE TABLE prompts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
  category_id UUID NOT NULL REFERENCES categories(id) ON DELETE CASCADE,
  content TEXT NOT NULL,
  is_active BOOLEAN DEFAULT TRUE NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
);

-- 监控结果表
CREATE TABLE monitoring_results (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
  prompt_id UUID NOT NULL REFERENCES prompts(id) ON DELETE CASCADE,
  ai_platform ai_platform NOT NULL,
  country_code TEXT NOT NULL,
  content JSONB NOT NULL,
  raw_response TEXT NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
);
```
