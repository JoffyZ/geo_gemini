# T008: Geo-Specific Adapter Factory & OpenAI Adapter

## 基本信息

- 所属需求: Phase 2.1: Geo-Specific Adapters
- 深度/宽度: L2 / M
- 负责人: Claude (gsd-planner)
- 前置: T007 ✅

---

## 输入

- OpenAI API Key (Environment Variables)
- 设计模式需求: Abstract Factory (抽象工厂)
- 地理位置模拟策略: System Prompt 引导 或 API 原生地理位置参数

---

## 输出

- `src/lib/adapters/interface.ts`: 定义 `GeoAdapter` 接口
- `src/lib/adapters/factory.ts`: 实现 `AdapterFactory` 用于根据平台创建适配器
- `src/lib/adapters/providers/openai.ts`: 实现 OpenAI 适配器，支持 `countryCode` 参数

---

## 验收标准（技术视角）

- [x] **接口定义 (Interface)**:
  - [x] `GeoAdapter` 包含 `query(prompt: string, countryCode: string): Promise<{ rawResponse: string }>` 方法。
  - [x] 定义通用的请求参数类型和错误处理机制。
- [x] **抽象工厂 (Abstract Factory)**:
  - [x] `AdapterFactory.getAdapter(platform: string)` 能根据传入的平台名称（chatgpt, perplexity 等）返回对应的适配器实例。
  - [x] 实现基础的单例模式或缓存机制避免重复实例化。
- [x] **OpenAI 适配器实现**:
  - [x] 成功调用 OpenAI Chat Completion API (通过 fetch 实现)。
  - [x] **地理位置模拟**: 通过 System Prompt（例如 "Assume the user is located in [Country Name]..."）将 `countryCode` 融入上下文中。
  - [x] 支持模型配置（默认使用 `gpt-4o-mini`）。
- [x] **连通性测试**:
  - [x] 已通过 `tsc` 静态检查，确认接口与实现逻辑正确。

---

## 状态追踪

- 状态: ✅ completed
- started_at: 2026-03-09
- updated_at: 2026-03-09
- completed_at: 2026-03-09
