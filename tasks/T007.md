# T007: Task Queue Infrastructure (BullMQ + Upstash)

## 基本信息

- 所属需求: Phase 1: Infrastructure & Data Model
- 深度/宽度: L2 / M
- 负责人: Rex
- 前置: T004 ✅

---

## 输入

- Upstash Redis 凭据
- `bullmq`, `ioredis` (Next.js Node.js Runtime)

---

## 输出

- `src/lib/queue/index.ts` 导出全局 `monitoringQueue` 实例
- `src/lib/queue/worker.ts` 基础 Worker 处理逻辑
- `src/app/api/jobs/test/route.ts` 用于测试队列连通性的 API

---

## 验收标准（技术视角）

- [x] BullMQ 连通性 (Upstash Connection)：
  - [x] `src/lib/redis.ts` 成功配置 `ioredisConnection` 且支持 TLS
  - [x] 通过 `ioredis` 配置 Upstash 专用的 `tls` 连接
- [x] 基础 Worker 骨架：
  - [x] `src/lib/queue/worker.ts` 定义了 `monitoringWorker` 处理 `ai-monitoring` 任务
  - [x] 支持按租户隔离的任务分发逻辑（在 `job.data` 中包含 `tenant_id`）
- [x] 错误与日志：
  - [x] 任务失败后自动重试机制 (Exponential Backoff: 3 attempts, 1s delay)
  - [x] 在日志中记录任务数据、状态及 `tenant_id`
- [x] 测试用例：
  - [x] API 端点 (`/api/jobs/test`) 成功向队列推送 Job 并在控制台打印 `Hello, Upstash!`

---

## 状态追踪

- 状态: ✅ completed
- started_at: 2026-03-09
- updated_at: 2026-03-09
- completed_at: 2026-03-09

---

## 结论

任务完成。已在 `src/lib/redis.ts` 中集成了 `ioredis` 用于连接 Upstash Redis（支持 TLS），并初始化了 BullMQ 队列及基础 Worker。提供了 `/api/jobs/test` 接口用于验证任务分发。
