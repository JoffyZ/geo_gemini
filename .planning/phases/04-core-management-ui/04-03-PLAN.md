---
phase: 04-core-management-ui
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: [src/db/schema.ts, src/lib/actions/monitoring-configs.ts, src/app/dashboard/settings/monitoring/page.tsx, src/components/monitoring/config-form.tsx]
autonomous: true
requirements: [4.2]

must_haves:
  truths:
    - "用户可以在界面上选择需要监测的国家列表"
    - "用户可以配置代理服务器信息"
    - "监测配置能够成功保存并持久化"
  artifacts:
    - path: "src/lib/actions/monitoring-configs.ts"
      provides: "Server Actions for Monitoring Config CRUD"
    - path: "src/app/dashboard/settings/monitoring/page.tsx"
      provides: "Monitoring Configuration View"
  key_links:
    - from: "src/app/dashboard/settings/monitoring/page.tsx"
      to: "src/lib/actions/monitoring-configs.ts"
      via: "Server Action form submission"
---

<objective>
实现多国家监测配置和代理配置界面，为后续的全球化监测任务提供基础。
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>T020: Monitoring Config Schema & Actions</name>
  <files>src/db/schema.ts, src/lib/actions/monitoring-configs.ts</files>
  <action>
    1. 在 `src/db/schema.ts` 中新增 `monitoring_configs` 表：
       - `id`: uuid
       - `tenantId`: uuid (reference tenants)
       - `name`: text (e.g. "Global Monitoring Plan")
       - `activeCountries`: jsonb (list of country codes)
       - `activeCategories`: jsonb (list of category ids)
       - `proxyConfig`: jsonb (host, port, username, password)
       - `isActive`: boolean
    2. 在 `src/lib/actions/monitoring-configs.ts` 中实现 CRUD Server Actions:
       - `getMonitoringConfigs()`, `createMonitoringConfig()`, `updateMonitoringConfig()`, `deleteMonitoringConfig()`.
  </action>
  <verify>
    使用测试脚本调用 `createMonitoringConfig` 并检查数据库结果。
  </verify>
  <done>
    多国家监测配置后端实现完成。
  </done>
</task>

<task type="auto">
  <name>T021: Multi-Country & Proxy Configuration UI</name>
  <files>src/app/dashboard/settings/monitoring/page.tsx, src/components/monitoring/config-form.tsx</files>
  <action>
    1. 使用 Shadcn UI 构建配置界面。
    2. 创建 `config-form.tsx`: 
       - 使用复选框列表 (Checkboxes) 或多选组件 (Multi-select) 选择国家。
       - 使用复选框列表选择参与监测的分类。
       - 输入框组配置代理信息 (Host, Port, User, Pass)。
    3. 在 `src/app/dashboard/settings/monitoring/page.tsx` 中渲染表单并调用 Server Actions 保存数据。
  </action>
  <verify>
    在浏览器中打开 `/dashboard/settings/monitoring`，修改国家和代理配置并保存，刷新页面后确认配置已持久化。
  </verify>
  <done>
    多国家及代理配置 UI 交付。
  </done>
</task>

</tasks>

<success_criteria>
1. 能够成功保存包含多个国家的监测计划。
2. 代理信息配置支持加密（如果是敏感信息，但在本阶段先实现持久化）。
3. 界面操作流畅，保存后有明确的反馈信息。
</success_criteria>
