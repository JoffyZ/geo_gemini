---
phase: 04-core-management-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.ts, src/lib/actions/prompts.ts, src/lib/validations/prompt.ts, src/app/dashboard/prompts/page.tsx, src/components/prompts/prompt-list.tsx, src/components/prompts/prompt-filters.tsx]
autonomous: true
requirements: [4.3]

must_haves:
  truths:
    - "用户可以在界面上查看所有监测问题"
    - "用户可以按分类和国家筛选问题"
    - "用户可以对问题进行 CRUD 操作"
  artifacts:
    - path: "src/lib/actions/prompts.ts"
      provides: "Server Actions for Prompt CRUD & Filter"
    - path: "src/app/dashboard/prompts/page.tsx"
      provides: "Prompt Library Main Page"
  key_links:
    - from: "src/app/dashboard/prompts/page.tsx"
      to: "src/lib/actions/prompts.ts"
      via: "Server Action calls with filters"
---

<objective>
实现问题库管理，支持 CRUD 操作以及按分类和国家进行多维筛选。
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>T018: Prompt CRUD Actions & Schema Update</name>
  <files>src/db/schema.ts, src/lib/actions/prompts.ts, src/lib/validations/prompt.ts</files>
  <action>
    1. 在 `src/db/schema.ts` 中更新 `prompts` 表，增加 `countryCode` 字段 (可选，text 类型)。
    2. 在 `src/lib/validations/prompt.ts` 中使用 zod 定义问题的校验 Schema (content, categoryId, countryCode)。
    3. 在 `src/lib/actions/prompts.ts` 中实现 Server Actions:
       - `getPrompts(filters)`: 获取问题列表，支持 `categoryId` 和 `countryCode` 筛选。
       - `createPrompt(data)`, `updatePrompt(id, data)`, `deletePrompt(id)`: 基础 CRUD 操作。
  </action>
  <verify>
    使用不同的筛选参数调用 `getPrompts` 并校验返回列表。
  </verify>
  <done>
    问题库 CRUD 及筛选逻辑后端实现完成。
  </done>
</task>

<task type="auto">
  <name>T019: Prompt Library UI (Filterable List)</name>
  <files>src/app/dashboard/prompts/page.tsx, src/components/prompts/prompt-list.tsx, src/components/prompts/prompt-filters.tsx</files>
  <action>
    1. 使用 Shadcn UI 组件构建列表界面。
    2. 创建 `prompt-filters.tsx`: 包含分类下拉列表 (Select) 和国家选择列表 (Select)。
    3. 创建 `prompt-list.tsx`: 展示问题列表，包含问题内容、所属分类和适用国家。
    4. 在 `src/app/dashboard/prompts/page.tsx` 中通过 URL query parameters 管理筛选状态，并调用 Server Actions 获取数据。
  </action>
  <verify>
    在浏览器中打开 `/dashboard/prompts`，选择不同分类或国家，确认列表能够根据筛选条件实时更新。
  </verify>
  <done>
    问题库管理 UI 交付，支持复杂筛选。
  </done>
</task>

</tasks>

<success_criteria>
1. 能够按分类成功筛选出相应的问题。
2. 能够按国家成功筛选出相应的问题。
3. 新增的问题能立即反映在筛选结果中。
</success_criteria>
