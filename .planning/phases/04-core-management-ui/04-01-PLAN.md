---
phase: 04-core-management-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.ts, src/lib/actions/categories.ts, src/lib/validations/category.ts, src/app/dashboard/categories/page.tsx, src/components/categories/category-form.tsx, src/components/categories/category-table.tsx]
autonomous: true
requirements: [4.1]

must_haves:
  truths:
    - "用户可以在界面上查看到所有已创建的分类"
    - "用户可以创建、修改和删除分类"
    - "分类数据持久化存储在 PostgreSQL 数据库中"
  artifacts:
    - path: "src/lib/actions/categories.ts"
      provides: "Server Actions for Category CRUD"
    - path: "src/app/dashboard/categories/page.tsx"
      provides: "Category Management Main Page"
  key_links:
    - from: "src/app/dashboard/categories/page.tsx"
      to: "src/lib/actions/categories.ts"
      via: "Server Action calls in React Server/Client Components"
---

<objective>
实现分类管理的核心功能，包括数据库操作、Server Actions 封装以及基于 Shadcn UI 的管理界面。
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>T016: Category CRUD Actions & Schema Validation</name>
  <files>src/lib/validations/category.ts, src/lib/actions/categories.ts</files>
  <action>
    1. 在 `src/lib/validations/category.ts` 中使用 zod 定义分类的校验 Schema (name, description)。
    2. 在 `src/lib/actions/categories.ts` 中实现以下 Server Actions:
       - `getCategories()`: 获取当前租户下的所有分类。
       - `createCategory(data)`: 创建新分类。
       - `updateCategory(id, data)`: 更新分类。
       - `deleteCategory(id)`: 删除分类。
    3. 确保所有操作都通过 `lib/auth.ts` 或 `lib/tenant-provider.tsx` 校验 `tenantId`。
  </action>
  <verify>
    使用测试脚本调用 `createCategory` 并检查数据库结果。
  </verify>
  <done>
    分类的 CRUD Server Actions 实现完成并通过基本逻辑验证。
  </done>
</task>

<task type="auto">
  <name>T017: Category Management View (Data Table & Forms)</name>
  <files>src/app/dashboard/categories/page.tsx, src/components/categories/category-form.tsx, src/components/categories/category-table.tsx</files>
  <action>
    1. 使用 Shadcn UI 组件 (Table, Dialog, Form, Input, Button) 构建界面。
    2. 创建 `category-table.tsx`: 展示分类列表，包含编辑和删除按钮。
    3. 创建 `category-form.tsx`: 分类创建/编辑的表单，支持表单校验。
    4. 在 `src/app/dashboard/categories/page.tsx` 中整合以上组件，并使用 Next.js 的缓存/重验证机制 (revalidatePath) 确保数据实时更新。
  </action>
  <verify>
    在浏览器中打开 `/dashboard/categories`，执行创建、编辑、列表查看和删除操作，确认功能正常且 UI 符合规范。
  </verify>
  <done>
    分类管理 UI 交付，实现完整的 CRUD 交互流程。
  </done>
</task>

</tasks>

<success_criteria>
1. 能够成功创建新分类并显示在列表中。
2. 修改分类描述后，列表能立即更新。
3. 删除操作有二次确认且能正确移除数据。
</success_criteria>
