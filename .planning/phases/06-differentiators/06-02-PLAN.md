---
phase: 06-differentiators
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/lib/ai/prompt-gen.ts, src/components/prompts/prompt-generator.tsx, src/app/api/ai/generate-prompts/route.ts]
autonomous: false
requirements: [6.2]

must_haves:
  truths:
    - "用户可以通过输入核心关键词生成多个符合不同营销阶段 (Marketing Funnel) 的 GEO 问题"
    - "生成的问题根据目标国家和文化背景进行了 Localized 优化"
    - "用户可以在 UI 中预览、编辑并一键导入生成的问题到问题库"
  artifacts:
    - path: "src/lib/ai/prompt-gen.ts"
      provides: "AI logic for localized prompt generation"
    - path: "src/components/prompts/prompt-generator.tsx"
      provides: "Shadcn UI component for prompt generation"
  key_links:
    - from: "src/components/prompts/prompt-generator.tsx"
      to: "/api/ai/generate-prompts"
      via: "fetch / SWR / React Query"
---

<objective>
利用 LLM 实现智能问题生成器，根据核心关键词和目标市场自动生成 localized GEO 问题，并提供 Shadcn UI 交互界面。
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-differentiators/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>T029: LLM-based Keyword Expansion & Funnel-based Question Generation</name>
  <files>src/lib/ai/prompt-gen.ts, src/app/api/ai/generate-prompts/route.ts</files>
  <action>
    1. 在 `src/lib/ai/prompt-gen.ts` 中实现两阶段生成逻辑：
       - **Step 1**: 关键词扩展。将种子词扩展为 10-15 个语义相关词。
       - **Step 2**: 意图引导生成。根据扩展词、目标国家和业务分类，生成 Awareness (What is), Consideration (Best...), Conversion (Review...) 阶段的问题。
    2. 使用 Zod 强制输出结构化的 JSON (问题列表、预期意图、建议分类)。
    3. 在 `src/app/api/ai/generate-prompts/route.ts` 中暴露 POST 接口，供前端调用。
  </action>
  <verify>
    使用 Postman/curl 调用接口，检查生成的 JSON 结构和内容的相关性及 Localization 质量。
  </verify>
  <done>
    AI 生成逻辑实现，接口调通。
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>T030: Localized Prompt Generator UI (Shadcn UI)</name>
  <files>src/components/prompts/prompt-generator.tsx</files>
  <what-built>
    一个美观的 Shadcn UI 问题生成界面，包含关键词输入、国家选择、分类选择、生成预览列表和导入功能。
  </what-built>
  <how-to-verify>
    1. 进入问题管理页面，点击 "AI 自动生成"。
    2. 输入核心词 "Best AI writing tool"，选择 "United States"。
    3. 点击生成，观察 Loading 状态及最终生成的卡片列表。
    4. 勾选感兴趣的问题，点击 "导入"，确认这些问题已出现在数据库中。
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
1. 生成的问题具有高度的相关性和地道的表达（Localization）。
2. 用户能够高效地通过 AI 扩展问题库，单次生成不超过 30s。
3. UI 符合 Shadcn 设计规范，交互流畅。
</success_criteria>
