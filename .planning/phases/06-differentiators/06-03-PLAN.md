---
phase: 06-differentiators
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/lib/classification/domain-classifier.ts, src/components/analytics/source-analysis.tsx, src/lib/actions/sources.ts]
autonomous: false
requirements: [6.3]

must_haves:
  truths:
    - "系统能够自动识别引用 URL 的域名并进行分类 (Media, Forum, Blog, Social...)"
    - "能够展示在特定分类和国家维度下的 Top 引用来源 (Top Sources) 排名"
    - "分类逻辑结合了静态黑白名单与 LLM 智能分类"
  artifacts:
    - path: "src/lib/classification/domain-classifier.ts"
      provides: "Logic for hybrid domain categorization"
    - path: "src/components/analytics/source-analysis.tsx"
      provides: "Shadcn UI component for Top Sources analysis"
  key_links:
    - from: "src/lib/actions/sources.ts"
      to: "monitoring_results"
      via: "Drizzle select and domain extraction"
---

<objective>
实现引用来源分类引擎和深度分析组件，揭示 AI 平台更倾向于从哪些类型的网站提取信息。
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-differentiators/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>T031: Domain Classification Engine (Hybrid Whitelist + LLM)</name>
  <files>src/lib/classification/domain-classifier.ts</files>
  <action>
    1. 在 `src/lib/classification/domain-classifier.ts` 中实现 `classifyDomain` 函数。
    2. **Whitelist**: 包含 Top 媒体、社交平台和行业知名站点的静态分类映射。
    3. **LLM Fallback**: 针对白名单外的域名，利用 GPT-4o-mini 根据域名及其常见特征进行自动分类。
    4. 实现 `src/lib/actions/sources.ts`：从 `monitoring_results` 的 `citations` jsonb 字段中提取所有域名并应用分类。
    5. 实现缓存机制，避免重复分类同一域名。
  </action>
  <verify>
    测试一组域名（如：reddit.com, nytimes.com, techcrunch.com, some-random-blog.net），检查返回的分类标签是否准确。
  </verify>
  <done>
    分类引擎实现，能够处理海量引用的快速分类。
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>T032: Top Sources Deep Analysis & Visualization (Shadcn UI)</name>
  <files>src/components/analytics/source-analysis.tsx, src/lib/actions/sources.ts</files>
  <what-built>
    一个深度分析组件，展示：
    - 引用来源分类占比饼图。
    - Top 10 引用域名列表及其对应的权威度/分类。
    - 支持按分类、平台和国家进行多维筛选。
  </what-built>
  <how-to-verify>
    1. 打开 "Dashboard" -> "Source Analysis"。
    2. 检查分类饼图是否正确展示了 Media vs Forum vs Social 的比例。
    3. 检查 Top Sources 列表是否符合常理（如在 US 地区看到更多的 Reddit 和知名媒体）。
    4. 切换分类和国家，验证图表数据的联动更新。
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
1. 引用域名分类覆盖率达到 95% 以上。
2. 数据可视化清晰直观，响应迅速。
3. 能够为用户揭示特定分类下的核心“背书”渠道。
</success_criteria>
