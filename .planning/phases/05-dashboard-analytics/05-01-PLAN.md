---
phase: 05-dashboard-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.ts, src/lib/actions/analytics.ts, src/lib/db/views.ts]
autonomous: true
requirements: [5.1]

must_haves:
  truths:
    - "数据能够按天、国家、分类和平台进行聚合"
    - "提及率 (Mention Rate) 和平均排名 (Avg Rank) 预计算完成"
    - "Server Actions 能够高效查询聚合后的指标数据"
  artifacts:
    - path: "src/lib/db/views.ts"
      provides: "SQL logic for Materialized Views"
    - path: "src/lib/actions/analytics.ts"
      provides: "Server Actions for dashboard data"
  key_links:
    - from: "src/lib/actions/analytics.ts"
      to: "brand_metrics_daily view"
      via: "Drizzle ORM select"
---

<objective>
实现后端指标聚合层，利用 PostgreSQL 物化视图预计算核心 KPI，并提供高性能的数据查询接口。
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>T022: Multi-Dimensional Metrics Aggregation (Materialized Views)</name>
  <files>src/lib/db/views.ts, src/db/schema.ts</files>
  <action>
    1. 在 `src/lib/db/views.ts` 中编写创建 `brand_metrics_daily` 物化视图的 SQL。
    2. 该视图需从 `monitoring_results` 表中提取 jsonb 里的品牌信息 (rank, name)，并关联 `prompts`, `categories` 和 `brands` 表。
    3. 聚合维度：`tenant_id`, `brand_id`, `category_id`, `country_code`, `ai_platform`, `date_trunc('day', created_at)`。
    4. 计算指标：`mention_count`, `total_mentions`, `avg_rank`, `share_of_voice` (mention_count / total_results)。
    5. 创建唯一索引以便支持 `REFRESH MATERIALIZED VIEW CONCURRENTLY`。
    6. 在 `src/db/schema.ts` 中使用 `pgTable` 定义该视图的 read-only mapping 结构。
  </action>
  <verify>
    手动执行刷新视图 SQL 并通过 SQL 查询校验 `brand_metrics_daily` 中的数据准确性。
  </verify>
  <done>
    物化视图定义完成，Drizzle Schema 映射完成。
  </done>
</task>

<task type="auto">
  <name>T023: Next.js Server Actions for Dashboard Metrics</name>
  <files>src/lib/actions/analytics.ts</files>
  <action>
    1. 在 `src/lib/actions/analytics.ts` 中实现 `getDashboardMetrics` Server Action。
    2. 支持参数：`dateRange` (start, end), `countries[]`, `categories[]`, `platforms[]`。
    3. 实现 `getTrendData`: 返回按日期排序的品牌提及率和排名趋势，适配 Recharts 数据格式。
    4. 实现 `getKPISummary`: 返回核心 KPI 指标（总提及次数、周环比变化、全平台平均排名）。
    5. 确保查询逻辑考虑到租户隔离 (`tenantId`)。
  </action>
  <verify>
    编写测试脚本调用 `getDashboardMetrics` 并检查返回的 JSON 结构。
  </verify>
  <done>
    分析层 Server Actions 实现完成，支持多维度过滤。
  </done>
</task>

</tasks>

<success_criteria>
1. 聚合查询响应时间在 200ms 以内。
2. 数据正确反映了 brands 表中定义的品牌及其在监测结果中的表现。
3. 物化视图支持并发刷新，不锁定读取操作。
</success_criteria>
