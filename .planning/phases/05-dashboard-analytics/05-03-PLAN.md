---
phase: 05-dashboard-analytics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/app/api/export/route.ts, src/lib/export/csv-generator.ts]
autonomous: true
requirements: [5.3]

must_haves:
  truths:
    - "用户可以根据选定的时间范围和维度导出监测结果数据"
    - "支持 CSV 格式导出，包含所有原始监测字段"
    - "导出过程通过流式处理 (Stream)，支持大数据量"
  artifacts:
    - path: "src/app/api/export/route.ts"
      provides: "Data export API endpoint"
    - path: "src/lib/export/csv-generator.ts"
      provides: "CSV generation utility"
---

<objective>
实现数据导出功能，支持将监测结果（含国家、分类、平台、品牌表现）导出为 CSV 文件。
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>T026: Data Export Service (CSV/Excel)</name>
  <files>src/app/api/export/route.ts, src/lib/export/csv-generator.ts</files>
  <action>
    1. 在 `src/lib/export/csv-generator.ts` 中实现 CSV 序列化逻辑，利用流式接口处理大数据。
    2. 在 `src/app/api/export/route.ts` 中定义 GET 接口。
    3. 支持过滤参数 (startDate, endDate, countryCodes, categoryIds)。
    4. 实现关联查询以获取品牌名称和分类名称，将其打平为 CSV 列。
    5. 设置正确的 HTTP Response Headers (`Content-Type: text/csv`, `Content-Disposition: attachment`).
  </action>
  <verify>
    使用 Postman 或浏览器访问 `/api/export`，确认下载的 CSV 文件内容正确，编码无乱码。
  </verify>
  <done>
    监测数据导出功能实现完成。
  </done>
</task>

</tasks>

<success_criteria>
1. 导出操作能够正确响应大规模数据集（>1000条记录）。
2. CSV 包含所有核心分析字段。
3. 导出接口具备基本的权限校验。
</success_criteria>
