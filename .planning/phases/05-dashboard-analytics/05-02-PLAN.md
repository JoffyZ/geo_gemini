---
phase: 05-dashboard-analytics
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/app/dashboard/page.tsx, src/components/dashboard/kpi-cards.tsx, src/components/dashboard/trend-charts.tsx, src/components/dashboard/dashboard-filters.tsx]
autonomous: true
requirements: [5.2]

must_haves:
  truths:
    - "用户可以查看包含提及次数、排名等关键指标的 KPI 卡片"
    - "多维度趋势图表支持交互式筛选 (时间、国家、分类)"
    - "可视化组件采用 Recharts 实现，数据流为异步加载"
  artifacts:
    - path: "src/components/dashboard/trend-charts.tsx"
      provides: "Visualization components using Recharts"
    - path: "src/app/dashboard/page.tsx"
      provides: "Main Dashboard Page"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/lib/actions/analytics.ts"
      via: "Server Action data fetching"
---

<objective>
构建 Dashboard 可视化层，包括多维度筛选器、核心 KPI 指标卡片以及动态趋势图表。
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-analytics/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>T024: Multi-Dimensional Dashboard Layout & Filters</name>
  <files>src/app/dashboard/page.tsx, src/components/dashboard/dashboard-filters.tsx</files>
  <action>
    1. 在 `src/app/dashboard/page.tsx` 中定义 Dashboard 整体布局。
    2. 实现 `dashboard-filters.tsx`: 
       - 时间选择器 (Date Range Picker)。
       - 平台/国家/分类的多选下拉框 (Multi-select)。
       - 通过 URL 状态 (Query Params) 或 Server Actions 触发数据重载。
    3. 集成 `getKPISummary` 获取顶部统计指标并展示。
  </action>
  <verify>
    打开 Dashboard 页面，点击筛选按钮并确认 URL 参数正确更新。
  </verify>
  <done>
    Dashboard 筛选架构和基础布局交付。
  </done>
</task>

<task type="auto">
  <name>T025: Interactive Trend Charts & Citation Breakdown</name>
  <files>src/components/dashboard/trend-charts.tsx, src/components/dashboard/kpi-cards.tsx</files>
  <action>
    1. 使用 Recharts 实现 `trend-charts.tsx`:
       - `MentionRateChart`: 线形图展示不同品牌的提及率趋势。
       - `AvgRankChart`: 阶梯图展示品牌排名变化。
    2. 实现 `kpi-cards.tsx`: 
       - 展示 "Mention Rate", "Share of Voice", "Avg Rank" 等卡片，支持环比数值展示。
    3. 支持悬停提示 (Tooltips) 和图例切换。
    4. 确保在数据加载过程中显示骨架屏 (Skeleton UI)。
  </action>
  <verify>
    查看可视化图表，确认坐标轴、标签和多品牌对比逻辑正确无误。
  </verify>
  <done>
    可视化图表组建交付，Dashboard 核心展示层完成。
  </done>
</task>

</tasks>

<success_criteria>
1. Dashboard 能够展示当前筛选条件下的实时趋势。
2. 跨平台/跨国家的品牌对比逻辑清晰。
3. UI 交互流畅，响应速度快。
</success_criteria>
